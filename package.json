{
  "name": "purchase-approvals",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo root — convenience scripts only (no workspace tool). backend/, frontend/ and mobile/ are independent packages.",
  "scripts": {
    "build:ci": "npm --prefix backend run build && npm --prefix frontend/host run build && npm --prefix frontend/solicitante run build && npm --prefix frontend/aprobador run build",
    "test:ci": "npm --prefix backend test && npm --prefix frontend/host test && npm --prefix frontend/solicitante test && npm --prefix frontend/aprobador test"
  }
}
