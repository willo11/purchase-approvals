# Local tooling artifacts (not source of truth)
.atl/

# Engram local memory (device-local, not source of truth)
.engram/

# Node
node_modules/
dist/
coverage/

# Serverless Framework deploy artifacts (created by `sls deploy`)
.serverless/

# Environment files — never commit secrets
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
*.log

# Build artifact (serverless-esbuild output)
.esbuild/
