change: purchase-approval-flow
artifact_store: openspec
status: in-progress
current_phase: archive
current_pr: 8

dag:
  proposal: done
  specs: done
  design: done
  tasks: done
  apply: done
  verify: done
  archive: in-progress

branch:
  base: main
  active: main
  applied_prs: [0, 1, 2, 3, 4, 5, 6, 7, 8]
  pending_prs: []

notes: |
  ALL 8 PRs MERGED — backend complete (#0-#5) + frontend complete (#6 requester-panel,
  #7 approver-flow) + folder refactor + release/docs (#8).
  VERIFY PASS-WITH-WARNINGS (obs #56): 29/29 spec scenarios with passing covering
  tests (incl. real concurrency races vs dynamodb-local: single completedAt,
  approve-vs-reject XOR, one-time OTP consume, atomic lockout); 65/65 tasks; design
  contract exact (CAS conditions, TTL-on-OTP-only, GSI1, validatedAt/401, evidenceKey
  idempotency, binaryMediaTypes); OpenAPI parseable + matches handlers; README runbook
  literally-followable; auth disclaimer present; English sweep clean (348 tests:
  backend 194 @93.56%, requester 46 @95.37%, approver 105 @98.31%; integration 221).
  Warning fixed post-verify: design-api RequestDetail shape aligned (evidenceKey, drop
  excluded). Accepted residuals: deploy documented-pending (no AWS creds in sandbox),
  validatedAt does not expire, PDF permanent-404 retry deferred (SQS evolution).
  NEXT: sdd-archive.

updated_at: 2026-08-15T03:00:00