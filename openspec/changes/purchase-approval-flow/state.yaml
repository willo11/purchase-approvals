change: purchase-approval-flow
artifact_store: openspec
status: in-progress
current_phase: apply
current_pr: 8

dag:
  proposal: done
  specs: done
  design: done
  tasks: done
  apply: in-progress
  verify: pending
  archive: pending

branch:
  base: main
  active: feat/release-docs
  applied_prs: [0, 1, 2, 3, 4, 5, 6, 7]
  pending_prs: [8]

notes: |
  PR #0 bootstrap merged. PR #1 user-registry MERGED. PR #2 purchase-request MERGED
  (aggregate + 4 ports, TransactWriteItems, unknown-email 404). PR #3 approver-otp
  MERGED (OTP gate: sha256, TTL item, in-code expiry, atomic lockout + consume CAS,
  mock-mail). PR #4 approval-signature MERGED (concurrency core: Step A + Step B CAS
  `!completedAt AND #status=pending`, reject first-wins, validatedAt/401, CI). PR #5
  pdf-evidence MERGED (PdfGenerator pdf-lib, S3EvidenceStore behind port, deterministic
  evidenceKey + idempotent guard, binaryMediaTypes fix, download 200/404).
  PRE-#7 refactor MERGED (5100cf4): per-remote folder convention applied to all 3 MF
  apps (app/api/components(+ui)/hooks/lib/pages/routes/store, colocated tests).
  PR #6 requester-panel MERGED (host shell + requester remote: list/create/detail,
  RHF+zod, Tailwind+shadcn, zustand scoped, English copy; CSS-through-exposed-module
  CRITICAL fixed).
  Delivery: PR per capability, 1 commit = 1 task, no line budget, git local.

  PR #7 approver-flow MERGED — LAST functional PR. Approver remote owns single /approve
  entry (?request_id&approver_token): gate-driven state machine mapped to HTTP codes
  (410/403/404/401/409) via lib/flow.js classifier; ApprovalLandingPage, OtpEntryPage,
  OtpLockedOutPage, ApprovalDecisionPage (approve w/o name, reject w/ confirm),
  TerminalPage variants; zustand flow store; folder convention + shadcn + English copy;
  standalone AND composed (host /approve). Fresh review: flow correct (verified every
  branch vs handlers); fixed CRITICAL focused-test guard extended to ALL frontends
  (frontend/scripts/no-focused-tests.mjs + pretest hooks, proven with temp .only);
  pinned 409/410 error-body contract in backend tests (classifier coupling now protected);
  retry affordance after transient action error; expiresInSeconds refreshed on regenerate.
  DECISIONS frontend #8. MANUAL-TESTING pending PR #7/#8 sections.

updated_at: 2026-08-15T02:00:00