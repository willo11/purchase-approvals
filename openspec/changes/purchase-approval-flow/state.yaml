change: purchase-approval-flow
artifact_store: openspec
status: in-progress
current_phase: apply
current_pr: 5

dag:
  proposal: done
  specs: done
  design: done
  tasks: done
  apply: in-progress
  verify: pending
  archive: pending

branch:
  base: main
  active: feat/pdf-evidence
  applied_prs: [0, 1, 2, 3, 4]
  pending_prs: [5, 6, 7, 8]

notes: |
  PR #0 bootstrap merged. PR #1 user-registry MERGED (82b71c1). PR #2 purchase-request
  MERGED (aggregate + 4 ports, TransactWriteItems create, unknown-email 404 fixed).
  PR #3 approver-otp MERGED (OTP gate: sha256 hash-only, own TTL item, in-code expiry,
  atomic lockout single UpdateItem, atomic one-time consume CAS, mock-mail inbox;
  concurrency tests vs dynamodb-local; DECISIONS #19 hardened + #20 CAS-vs-idempotency
  + #21 mock-mail-as-outbox).
  Delivery: PR per capability, 1 commit = 1 task, no line budget, git local.

  PR #4 approval-signature MERGED — the concurrency core: shared ApproverGate with 4th
  check (already-acted → 409); approve = Step A per-approver CAS (no double-sign) + Step B
  REQ-level completion CAS `attribute_not_exists(completedAt) AND #status = :pending`;
  reject = Step A + Step B CAS `status=PENDING AND !rejectedAt` (first-reject-wins);
  completed XOR rejected by construction; signature = registered snapshot name + timestamp
  (R1); validated-OTP precondition enforced via durable validatedAt marker → 401;
  EvidenceGeneratorPort defined (stub; real PDF PR #5); ConsistentRead on completion trigger.
  Fresh review caught + fixed CRITICAL: completeIfAbsent lacked status=PENDING → reject-then-
  complete could set both flags (probe: 8/20). Fixed symmetric CAS + repo-level race test
  (exactly-one-winner). CI added (.github/workflows/ci.yml with dynamodb-local + anti-.only
  guard since Jest 29 dropped forbidOnly). DECISIONS #22. MANUAL-TESTING PR #4 section.

updated_at: 2026-08-14T23:00:00